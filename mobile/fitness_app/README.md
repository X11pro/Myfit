# fitness_app

Base Flutter de `Myfit`.

## Estado

Se dejo preparado el scaffold principal de Dart/Flutter, pero esta maquina no tiene `flutter` ni `dart` en el `PATH`, asi que no fue posible ejecutar `flutter create`, `flutter pub get` ni generar carpetas nativas `android/` e `ios`.

## Cuando Flutter este instalado

Desde esta carpeta ejecutar:

```powershell
flutter create .
flutter pub get
flutter analyze
```

Si `flutter create .` intenta sobreescribir archivos, usar una copia de seguridad o regenerar solo plataformas nativas.
